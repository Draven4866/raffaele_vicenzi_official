---
/**
 * MENU-02 — BIO — Raffaele Vicenzi Autore — v1.2
 *
 * Modifica v1.2:
 * - SOLO box: ridotta la larghezza della card (.bioCard) per eliminare lo spazio vuoto a destra.
 * - Tutto il resto (testo/HTML/CSS/animazioni) invariato.
 *
 * Note tecniche:
 * - URL asset assoluti via BASE_URL (u()).
 * - Font via @font-face INIETTATI in <head> (niente url("fonts/...") nel CSS estratto).
 * - Layout bio: card trasparente sopra background + foto “dentro” al testo (float) + reveal on scroll.
 */

// ===== BASE URL (subpath safe) =====
const baseHref = import.meta.env.BASE_URL.endsWith("/")
  ? import.meta.env.BASE_URL
  : import.meta.env.BASE_URL + "/";

const u = (p: string): string => baseHref + p.replace(/^\/+/, "");

// ===== LINKS MENU (come index) =====
const hrefHome = baseHref;
const hrefM02 = u("menu-02/");
const hrefM03 = u("menu-03/");
const hrefM04 = u("menu-04/");
const hrefM05 = u("menu-05/");
const hrefM06 = u("menu-06/");

// ===== ASSET =====
const profileImg = u("img/profilo.jpg");
const heroBgUrl = u("img/hero.jpg");

// ===== FONTS (public/fonts) =====
const candFontUrl = u("fonts/candara.ttf");
const mrBFontUrl = u("fonts/MrB.ttf");
const dsAccentFontUrl = u("fonts/DSAccent.ttf");

const fontFaceCSS = `
@font-face{
  font-family:"Candara";
  src:url("${candFontUrl}") format("truetype");
  font-weight:400;
  font-style:normal;
  font-display:swap;
}
@font-face{
  font-family:"MrB";
  src:url("${mrBFontUrl}") format("truetype");
  font-weight:400;
  font-style:normal;
  font-display:swap;
}
@font-face{
  font-family:"DSAccent";
  src:url("${dsAccentFontUrl}") format("truetype");
  font-weight:400;
  font-style:normal;
  font-display:swap;
}
`;
---

<!doctype html>
<html lang="it">
  <head>
    <meta charset="utf-8" />
    <base href={baseHref} />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Raffaele Vicenzi — Bio</title>

    <link rel="preload" href={candFontUrl} as="font" type="font/ttf" crossorigin />
    <link rel="preload" href={mrBFontUrl} as="font" type="font/ttf" crossorigin />
    <link rel="preload" href={dsAccentFontUrl} as="font" type="font/ttf" crossorigin />

    <style is:global set:html={fontFaceCSS}></style>
  </head>

  <body>
    <!-- TOPBAR (uguale a index) -->
    <header class="topbar">
      <a class="brand" href={hrefHome} aria-label="Home">
        <img class="brandImg" src={profileImg} alt="" width="52" height="52" />
        <span class="brandText">
          <span class="brandName">Raffaele Vicenzi</span>
          <span class="brandLine">| Autore | Esoterista | Sciamano | Artista-Artigiano |</span>
        </span>
      </a>

      <nav class="nav" aria-label="Menu">
        <a class="navItem" href={hrefHome}>Home</a>
        <a class="navItem isActive" href={hrefM02} aria-current="page">Bio</a>
        <a class="navItem" href={hrefM03}>Il Campo Del Cuore</a>
        <a class="navItem" href={hrefM04}>Arte Sciamanica</a>
        <a class="navItem" href={hrefM05}>Esperienze</a>
        <span class="navItem navDisabled" aria-disabled="true" title="In arrivo">Contatti</span>
      </nav>
    </header>

    <main>
      <!-- BACKGROUND + CARD (livello 2) -->
      <section class="bioStage" style={`--stageBkg: url('${heroBgUrl}');`}>
        <div class="bioCard" role="region" aria-label="Biografia">
          <header class="bioHead">
            <h1 class="bioTitle">Biografia</h1>
            <p class="bioSubtitle">Autore, esoterista e artigiano del simbolo: il percorso che mi ha portato qui</p>
          </header>

          <div class="bioBody">
            <!-- FOTO “dentro al testo”: float => il testo ci gira attorno -->
            <img class="floatPhoto" src={profileImg} alt="Raffaele Vicenzi" width="220" height="220" />

            <article class="bioBlock" data-reveal>
              <h2>Identità e postura</h2>
              <p>
                Mi chiamo Raffaele e sono uno scrittore con formazione artistica, oltre che uno studioso appassionato di esoterismo.
                Non sono un dottore, né uno scienziato, né un “guru”. Il mio lavoro parte da una scelta semplice: trattare ciò che è interiore
                con rispetto, lucidità e concretezza, senza spettacolarizzarlo e senza trasformarlo in un’etichetta identitaria.
              </p>
            </article>
            <br>
            <article class="bioBlock" data-reveal>
              <h2>Scrittura come tecnologia di traduzione</h2>
              <p>
                La scrittura mi accompagna da sempre: è stata prima un appiglio immaginifico e poi una disciplina. Nel tempo ho imparato a usarla
                come strumento di traduzione: dare forma a ciò che preme dentro, ordinare il caos e costruire linguaggio laddove prima c’era solo intensità.
              </p>
            </article>
            <br>
            <article class="bioBlock" data-reveal>
              <h2>Formazione artistica e primo corpus</h2>
              <p>
                Mi sono laureato presso l’Università Ca’ Foscari di Venezia in Tecniche Artistiche dello Spettacolo (T.A.R.S.). Dopo la laurea, nel 2010,
                ho rivisto alcuni scritti composti tra il 1998 e il 2005: una raccolta di poesie e una serie di racconti brevi. Quel lavoro di revisione
                è stato un atto fondativo.
              </p>
            </article>
            <br>
            <article class="bioBlock" data-reveal>
              <h2>Musica e dimensione internazionale</h2>
              <p>
                Dal 2010 mi sono dedicato anche alla musica con un progetto hard rock/metal. Quel periodo mi ha portato in contesti di produzione professionale
                e in studi di registrazione, fino a una fase internazionale culminata con un debut album pubblicato ufficialmente nell’agosto del 2012.
              </p>
            </article>
            <br>
            <article class="bioBlock" data-reveal>
              <h2>Ritorno alla scrittura e formazione narrativa</h2>
              <p>
                Dal 2015 ho scelto di dedicarmi pienamente alla scrittura. Nel 2016 un incontro formativo con Franco Forte mi ha aiutato a mettere a terra
                metodo e costanza. A metà 2018 sono stato selezionato per il Master in Tecniche della Narrazione della <a href="https://www.scuolapalomar.it" target="_blank" rel="noopener noreferrer">Scuola Paolmar</a>
 (2018/2019).
              </p>
            </article>
            <br>
            <article class="bioBlock" data-reveal>
              <h2>Dalla sensibilità alla competenza di campo</h2>
              <p>
                Negli ultimi anni la mia ricerca si è concentrata su un passaggio specifico: trasformare la sensibilità in competenza.
                Ho trovato un contesto di lavoro serio e pratico in <a href="https://www.fucinalchemica.it" target="_blank" rel="noopener noreferrer">FucinAlchemica</a>
.
              </p>
            </article>
            <br>
            <article class="bioBlock" data-reveal>
              <h2>Integrazione corpo–psiche</h2>
              <p>
                Ho integrato la dimensione somatica e il lavoro sulle radici: pratiche di radicamento presso <a href="https://raidho-healinghorses.com" target="_blank" rel="noopener noreferrer">Raidho Healing Horses</a>
 e un percorso sul bambino interiore
                con Cinzia Maino di <a href="https://orionmultidimensional.com" target="_blank" rel="noopener noreferrer">Orion Multi Dimenzsional</a>
.
              </p>
            </article>
            <br>
            <article class="bioBlock" data-reveal>
              <h2>Campo Emozionale</h2>
              <p>
                Da questo intreccio nasce il concetto di Campo Emozionale: un modo per rendere leggibile ciò che il Corpo Emozionale “emana” nella vita quotidiana,
                come mappa utile e struttura pratica.
              </p>
            </article>

            <div class="clearFloat" aria-hidden="true"></div>
          </div>
        </div>
      </section>
    </main>

    <footer class="footer">
      <small>© <span id="y"></span> Raffaele Vicenzi</small>
    </footer>

    <!-- Year + Reveal on scroll (blocchi bio) -->
    <script is:inline>
      (() => {
        const y = document.getElementById("y");
        if (y) y.textContent = String(new Date().getFullYear());

        const blocks = Array.from(document.querySelectorAll("[data-reveal]"));
        if (!("IntersectionObserver" in window) || blocks.length === 0) {
          blocks.forEach((b) => b.classList.add("isIn"));
          return;
        }

        const io = new IntersectionObserver(
          (entries) => {
            entries.forEach((e) => {
              if (e.isIntersecting) {
                e.target.classList.add("isIn");
                io.unobserve(e.target);
              }
            });
          },
          { threshold: 0.18 }
        );

        blocks.forEach((b) => io.observe(b));
      })();
    </script>

    <style>
      :root{
        --ink:#0b0b0b;

        /* Bio “azzurrino” */
        --bioPaper:#eaf3ff;
        --bioCard: rgba(255,255,255,0.62);
        --bioBorder: rgba(0,0,0,0.12);

        /* hover verdino menu */
        --menuHover: rgba(119, 194, 99, 0.28);
      }

      *{ box-sizing:border-box; }
      body{
        margin:0;
        color: var(--ink);
        font-family: "Candara", system-ui, -apple-system, Segoe UI, Roboto, Arial;
        background: var(--bioPaper);
        line-height:1.45;
      }
      a{ color:inherit; text-decoration:none; }

      /* TOPBAR (uguale a index) */
      .topbar{
        position: sticky;
        top:0;
        z-index: 50;
        display:flex;
        justify-content: space-between;
        align-items: center;
        padding: 12px 18px;
        background: rgba(255,255,255,0.92);
        border-bottom: 1px solid rgba(0,0,0,0.08);
        backdrop-filter: blur(10px) saturate(180%);
        gap: 14px;
      }
      .brand{
        display:flex;
        align-items:center;
        gap: 12px;
        min-width: 220px;
      }
      .brandImg{
        width: 52px;
        height: 52px;
        border-radius: 12px;
        object-fit: cover;
        border: 1px solid rgba(0,0,0,0.20);
        background: #fff;
        flex: 0 0 auto;
      }
      .brandText{ display:flex; flex-direction: column; line-height: 1.1; }
      .brandName{
        font-size: 18px;
        font-weight: 600;
        letter-spacing: 0.01em;
      }
      .brandLine{
        margin-top: 6px;
        font-family:"MrB","Candara",system-ui,sans-serif;
        font-size: 12px;
        opacity: 0.92;
        letter-spacing: 0.03em;
        white-space: nowrap;
      }

      .nav{
        display:flex;
        align-items:center;
        gap: 10px;
        flex-wrap: wrap;
        justify-content: flex-end;
      }
      .navItem{
        padding: 10px 12px;
        border-radius: 999px;
        background: rgba(0,0,0,0.05);
        border: 1px solid rgba(0,0,0,0.10);
        font-size: 14px;
        transition: background-color .16s ease, transform .16s ease, filter .16s ease;
        position: relative;
        will-change: transform;
      }
      .navItem:not(.navDisabled):hover{
        background: var(--menuHover);
        animation: navShakeY 0.65s ease-in-out infinite;
      }
      .navItem:focus-visible{
        outline: 2px solid rgba(0,0,0,0.55);
        outline-offset: 3px;
      }
      .navDisabled{
        opacity: 0.45;
        cursor: default;
        user-select:none;
        background: rgba(0,0,0,0.03);
      }
      .isActive{
        background: rgba(120,170,255,0.20);
        border-color: rgba(0,0,0,0.10);
      }

      @keyframes navShakeY{
        0%   { transform: translateY(0); }
        25%  { transform: translateY(-2px); }
        50%  { transform: translateY(0); }
        75%  { transform: translateY(2px); }
        100% { transform: translateY(0); }
      }

      /* STAGE */
      .bioStage{
        position: relative;
        min-height: calc(100vh - 72px);
        padding: 42px 18px 56px;
        background: var(--bioPaper);
      }

      /* background “bkg” dietro la card */
      .bioStage::before{
        content:"";
        position:absolute;
        inset:0;
        background-image: var(--stageBkg);
        background-size: cover;
        background-position: center;
        opacity: 0.18;
        filter: saturate(1.05) contrast(1.02);
        pointer-events:none;
      }
      .bioStage::after{
        content:"";
        position:absolute;
        inset:0;
        background: linear-gradient(to bottom, rgba(255,255,255,0.45), rgba(234,243,255,0.92));
        pointer-events:none;
      }

      .bioCard{
        position: relative;
        z-index: 1;

        /* ✅ v1.2 — SOLO QUI: box più stretta, centrata */
        max-width: 800px;   /* prima 1020px */
        width: 100%;

        margin: 0 auto;
        border-radius: 22px;
        background: var(--bioCard);
        border: 1px solid var(--bioBorder);
        backdrop-filter: blur(12px) saturate(160%);
        box-shadow: 0 18px 55px rgba(0,0,0,.12);
        overflow: hidden;
      }

      .bioHead{
        padding: 20px 20px 10px;
        border-bottom: 1px solid rgba(0,0,0,0.08);
        background: rgba(255,255,255,0.30);
      }

      .bioTitle{
        margin: 0;
        font-family:"DSAccent","Candara",system-ui,sans-serif;
        font-weight: 400;
        font-size: 34px;
        line-height: 1.05;
      }
      .bioSubtitle{
        margin: 8px 0 0;
        font-family:"MrB","Candara",system-ui,sans-serif;
        font-size: 18px;
        opacity: 0.92;
        max-width: 70ch;
      }

      .bioBody{
        padding: 18px 20px 22px;
      }

      /* FOTO “nel testo” */
      .floatPhoto{
        float: left;
        width: 220px;
        height: 220px;
        object-fit: cover;
        border-radius: 18px;
        margin: 6px 18px 10px 0;
        border: 1px solid rgba(0,0,0,0.16);
        box-shadow: 0 16px 40px rgba(0,0,0,.14);
        background: rgba(255,255,255,0.50);
      }

      .bioBlock{
        opacity: 0;
        transform: translateY(14px);
        transition: opacity .55s ease, transform .55s ease;
      }
      .bioBlock.isIn{
        opacity: 1;
        transform: translateY(0);
      }

      .bioBlock h2{
        margin: 0 0 8px;
        font-family:"DSAccent","Candara",system-ui,sans-serif;
        font-weight: 400;
        font-size: 22px;
      }
      .bioBlock p{
        margin: 0 0 16px;
        max-width: 85ch;
        text-align: justify;
        text-justify: inter-word;
        hyphens: auto;
      }

      .clearFloat{ clear: both; }

      /* FOOTER */
      .footer{
        padding: 22px 18px;
        background: rgba(255,255,255,0.85);
        border-top: 1px solid rgba(0,0,0,0.10);
      }

      @media (max-width: 900px){
        .brandLine{ white-space: normal; }
        .floatPhoto{
          float: none;
          display: block;
          width: min(320px, 100%);
          height: auto;
          aspect-ratio: 1 / 1;
          margin: 6px 0 14px;
        }
        .bioBody{ padding: 16px 16px 18px; }
        .bioHead{ padding: 18px 16px 10px; }
      }
    </style>
  </body>
</html>
